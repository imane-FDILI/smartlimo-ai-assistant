# Minimal dependency set for the FastAPI service (backend/app).
# Generated by auditing actual `import` statements under backend/app — NOT a
# `pip freeze`. Streamlit/dashboard-only packages (streamlit, altair, plotly,
# folium, pyarrow, GitPython, openpyxl, stripe, pydeck, ...) live in the
# dashboard's own requirements file, not here.
#
# Versions pinned to match what was already tested in the original
# requirements.txt (pip freeze from the dev machine).

fastapi==0.139.0
uvicorn==0.50.2
pydantic==2.13.4
starlette==1.3.1

SQLAlchemy==2.0.51
psycopg2-binary==2.9.12
python-dotenv==1.2.2

# NLP / entity extraction (app/ai/entity_extractor.py)
spacy==3.8.14
en_core_web_sm @ https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl#sha256=1932429db727d4bff3deed6b34cfc05df17794f4a52eeb26cf8928f7c1a0fb85

# Intent/recommender models are pickled scikit-learn pipelines (app/ai/*.joblib)
# loaded via joblib.load() -> scikit-learn must be installed even though it's
# never imported directly in app/.
scikit-learn==1.9.0
joblib==1.5.3

# Natural-language date/time parsing + US holiday surcharge rules
# (app/services/reservation_service.py)
dateparser==1.4.1
holidays==0.103

# Geocoding / routing calls to Nominatim + OSRM (app/services/geo_service.py)
requests==2.34.2

# app/import_pricing.py (manual, one-off script; reads the pricing .xlsx files)
pandas==3.0.3
openpyxl==3.1.5
